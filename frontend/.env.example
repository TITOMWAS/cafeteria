# Cloudflare Turnstile captcha (https://dash.cloudflare.com -> Turnstile).
# Leave blank in dev — the challenge widget stays hidden and no token is sent.
VITE_TURNSTILE_SITE_KEY=

# Backend API base (defaults to http://localhost:5001 in dev)
# VITE_API_URL=https://api.your-domain.com